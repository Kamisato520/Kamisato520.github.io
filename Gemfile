source "https://rubygems.org"

# This site is built with the github-pages gem, exactly like GitHub Pages does.
# For local preview on Windows without Ruby, use the official Docker image:
#   docker run --rm -p 4000:4000 -v "$($PWD.Path):/srv/jekyll" jekyll/jekyll:pages jekyll serve --force_polling

gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
